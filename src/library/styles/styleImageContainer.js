import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  card: {
    width: '100%',
    height: 250,
  },
  image: {
    width: '100%',
    height: 250,
    borderRadius: 35,
  },
});
